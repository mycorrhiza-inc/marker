services:
  marker-1:
    build: .
    mem_limit: 50G
    memswap_limit: 50G
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0"]
              capabilities: [gpu]
    env_file:
      - ./.env
    command: "python3 new_server.py"
    restart: always
